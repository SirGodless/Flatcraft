{
  "name": "@flatcraft/content",
  "version": "0.1.0",
  "description": "Content-package discovery: loose directories and .zip archives under a content/ root, each with a content.json manifest. No game-content knowledge (blocks/items/...) - just \"here's a package id, version, and file tree\", consumed by the sim/dedicated/client registration layers.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "esbuild": "^0.24.0",
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "vitest": "^2.1.0"
  }
}
