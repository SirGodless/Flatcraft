{
  "name": "@flatcraft/dedicated",
  "version": "0.1.0",
  "description": "Headless dedicated server (Node): serves the browser client, WebSocket game transport, world persistence, accounts.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts --bundle --platform=node --target=node20 --format=esm --outfile=dist/server.mjs --banner:js=\"import { createRequire as __flatcraftCreateRequire } from 'node:module'; const require = __flatcraftCreateRequire(import.meta.url);\" --external:bufferutil --external:utf-8-validate --external:isolated-vm --external:esbuild",
    "start": "npm run build && node dist/server.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@flatcraft/content": "*",
    "@flatcraft/server": "*",
    "@flatcraft/sim": "*",
    "isolated-vm": "^6.2.0",
    "openid-client": "^6.8.5",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.5.0",
    "esbuild": "^0.24.0",
    "vitest": "^2.1.0"
  }
}
