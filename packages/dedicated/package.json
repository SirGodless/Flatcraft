{
  "name": "@flatcraft/dedicated",
  "version": "0.1.0",
  "description": "Headless dedicated server entry point (Node). No rendering, no DOM. Networking arrives once the singleplayer core is done.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "module",
  "scripts": {
    "start": "node --experimental-strip-types src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@flatcraft/server": "*",
    "@flatcraft/sim": "*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  }
}
